services:
  vault-sync:
    image: ghcr.io/alexjbarnes/vault-sync:latest
    env_file: .env
    ports:
      - "8090:8090"
    volumes:
      - data:/root/.vault-sync
    restart: unless-stopped

volumes:
  data:
